# If you have a list named people, how can you determine the number of entries in that list?

You can take the name of the list and pass it through len()

Ex: len(list_name), or in the example from above, len(people)