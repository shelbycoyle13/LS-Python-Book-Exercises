# To what value does the following expression evaluate?

# 'foo' == 'Foo'

This would evaluate False because the two strings are different. For strings to be equal, they have to have the same case. This comparison is case-sensitive.