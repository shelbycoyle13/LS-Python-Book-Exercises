# Identify all of the identifiers on each line of the following code.

# def multiply(left, right):    #multiply = name of function; left and right are the parameters
#     return left * right       

# def get_num(prompt):          #get_num = name of function; prompt = parameter
#     return float(input(prompt))   #float = name of built-in float function; input = name of built-in input function

# first_number = get_num('Enter the first number: ')    #first_number = variable
# second_number = get_num('Enter the second number: ')  #second_number = variable
# product = multiply(first_number, second_number)       #product = variable; first_number and second_number are now arguments here
# print(f'{first_number} * {second_number} = {product}')  #print = name of built-in print function

